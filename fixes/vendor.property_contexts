# OnePlus 9 Pro vendor property-context overrides.
# Intentionally empty until a target-specific property needs an explicit SELinux
# mapping. The original file contained OnePlus 13 ultrasonic-FOD/status-bar mappings.
